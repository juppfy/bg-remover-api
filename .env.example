# Copy to .env and fill in your own values. Never commit .env or real secrets.

# Required: API key for X-API-Key header (use a strong random string in production)
API_KEY=your-secure-api-key-here

# --- Railway Storage (from your bucket's Credentials tab) ---
# Railway gives you: Bucket name + Endpoint URL (https://storage.railway.app).
# Buckets are private; the API returns presigned URLs (valid 7 days).

# Option A: Railway variable names (for Variable References from the bucket)
BUCKET=your-bucket-name-xxxx
ENDPOINT=https://storage.railway.app
ACCESS_KEY_ID=your-access-key-id
SECRET_ACCESS_KEY=your-secret-access-key
REGION=auto

# Option B: Alternative names (set manually from Credentials tab)
# RAILWAY_BUCKET_NAME=your-bucket-name-xxxx
# RAILWAY_BUCKET_ENDPOINT=https://storage.railway.app
# RAILWAY_BUCKET_ACCESS_KEY=your-access-key-id
# RAILWAY_BUCKET_SECRET_KEY=your-secret-access-key

# Server port (Railway sets PORT automatically)
PORT=8000
